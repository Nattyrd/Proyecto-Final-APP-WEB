-- Script inicial para PostgreSQL (Grupo 5 - E-commerce API)
-- La base de datos 'ecommerce' se crea via POSTGRES_DB en docker-compose.
-- Las tablas son gestionadas por GORM AutoMigrate al iniciar la API.

SELECT 'Base de datos ecommerce lista para migraciones GORM' AS status;
